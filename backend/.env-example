# Server — loaded via dotenv (npm install dotenv)
PORT=3000

# Database — Prisma reads this to connect to PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/employee_scheduling

# Auth — used to sign and verify JWT tokens
JWT_SECRET=change-me-to-something-secret
